#!/bin/bash

# cd $1
sudo docker-compose run backend django-admin startproject project
# sudo docker-compose run frontend vue init nuxt-community/starter-template project .
# sudo docker-compose run frontend npm install
